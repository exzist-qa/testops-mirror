# Allure TestOps base URL (no trailing slash)
TESTOPS_ENDPOINT=https://testops.example.com

# API token generated in Allure TestOps → Profile → API tokens
TESTOPS_TOKEN=your-api-token-here

# Custom field name used to derive folder structure (default: Suite)
TESTOPS_SUITE_FIELD=Suite
